use flake

# Workspace-specific context prompt injected by cogitator on every turn.
# Points outside the cogitator repo so workspace-sensitive content is never
# committed to the public tool repo.
export COGITATOR_WORKSPACE_CONTEXT_PATH=/home/kaddu/Projects/docs/workspace-context.md
